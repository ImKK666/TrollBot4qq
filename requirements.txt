# TrollBot 项目依赖
# 核心框架
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# 网络通信
httpx>=0.25.0
websockets>=12.0

# AI和机器学习
openai>=1.3.0
chromadb>=0.4.15

# 数据处理
sqlite3  # Python内置模块，无需安装

# 类型检查和工具（可选，用于开发）
pydantic>=2.0.0  # FastAPI依赖
typing-extensions>=4.8.0

# 异步支持
asyncio  # Python内置模块

# 其他工具
python-multipart>=0.0.6  # FastAPI文件上传支持（如需要）
python-dotenv>=1.0.0     # 环境变量支持（可选）

# 开发和测试工具（可选）
pytest>=7.4.0
pytest-asyncio>=0.21.0
black>=23.0.0
isort>=5.12.0 